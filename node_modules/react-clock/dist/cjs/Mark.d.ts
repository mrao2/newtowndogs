import React from 'react';
import type { MarkLength, MarkWidth } from './shared/types.js';
type MarkProps = {
    angle?: number;
    length?: MarkLength;
    name: string;
    number?: React.ReactNode;
    width?: MarkWidth;
};
export default function Mark({ angle, length, name, width, number }: MarkProps): JSX.Element;
export {};
